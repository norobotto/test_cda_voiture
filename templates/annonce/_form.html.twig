{{ form_start(form) }}
<div class="col w-75">{{ form_row(form.title) }}</div>
<div class="col w-75">{{ form_row(form.description) }}</div>
<div class="col w-75">{{ form_row(form.model) }}</div>
<div class="col w-75">{{ form_row(form.price) }}</div>
<div class="col w-75">{{ form_row(form.year) }}</div>
<div class="col w-75">{{ form_row(form.km) }}</div>
<div class="col w-75">{{ form_row(form.fuel) }}</div>
{# <div class="col w-75">{{ form_row(form.license) }}</div> #}
<div class="col w-75">{{ form_row(form.imgfile) }}</div>
{# <div class="col w-75">{{ form_row(form.is_visible) }}</div> #}
<div class="col w-75">{{ form_row(form.marque) }}</div>
{# <div class="col w-75">{{ form_row(form.author) }}</div> #}
{# {{ form_widget(form) }} reprend tous les add du form/type #}
<div class="d-flex justify-content-start mt-3">
    <button class="btn btn-info">{{ button_label|default('Save') }}</button>
</div>
{{ form_end(form) }}
